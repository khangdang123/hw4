def sort_dictionary(dict):
    sorted_list = []

    for name, (phone_number, age) in sorted_list(dict.items(), key = lambda x: x[1][1]):

      sorted_list.append((name, phone_number))

      return sorted_list
